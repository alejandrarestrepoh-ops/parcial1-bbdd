services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: parcial_app
    restart: unless-stopped
    ports:
      - "8090:80"
    volumes:
      - ./src:/var/www/html
    networks:
      - parcial_network
    depends_on:
      - db

  db:
    image: mariadb:10.8
    container_name: parcial_mariadb
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root_password
      MYSQL_DATABASE: biblioteca_db
      MYSQL_USER: dev_user
      MYSQL_PASSWORD: dev_password
    ports:
      - "3307:3306"
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - parcial_network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: parcial_phpmyadmin
    restart: unless-stopped
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: root_password
      UPLOAD_LIMIT: 100M
    ports:
      - "8091:80"
    networks:
      - parcial_network
    depends_on:
      - db

volumes:
  db_data:

networks:
  parcial_network:
    driver: bridge
